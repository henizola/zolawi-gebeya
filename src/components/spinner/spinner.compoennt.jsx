import React from 'react';

import { Overlay, SpinnerContainer } from './spinner.styles';

const Spinner = () => {
	return (
		<Overlay>
			<SpinnerContainer></SpinnerContainer>
		</Overlay>
	);
};
export default Spinner;
